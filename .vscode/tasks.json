{
    "tasks": [
        {
            "type": "shell",
            "label": "Build ZombieGame",
            "command": "make",
            "args": ["config=debug_x64"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}